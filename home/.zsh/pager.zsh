# Syntax highlight for less with 'source-highlite'
PAGER='less -X -M'
#export LESSOPEN="|/usr/local/bin/lesspipe.sh %s" # not compatible with rvm, it only prints the gem and gemset
export LESS=' -R '
